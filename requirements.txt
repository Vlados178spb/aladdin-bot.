# ======================================================
#  A L A D D I N - B O T
#  Супер-спортивный бот с самообучением
#  Версия: Ultimate Performance + AI
# ======================================================

# ----- Исправленные библиотеки из твоего исходного requirements.txt -----
aiogram==3.12.0                     # вместо "аиограмма" — асинхронный фреймворк для Telegram
aiohttp==3.11.12                    # асинхронные HTTP-запросы (API коэффициентов)
pydantic==2.10.6                    # вместо "пидантический" — валидация данных
pytest==8.3.5                       # вместо "пыц" — тестирование
deep-translator==1.11.4             # вместо "глубинный переводчик" — перевод текстов (например, новостей команд)
python-dotenv==1.0.1                # переменные окружения

# ----- ЯДРО ПРОИЗВОДИТЕЛЬНОСТИ (2000+ матчей / сек) -----
uvloop==0.20.0                      # ускоряет asyncio в 2 раза
aiodns==3.2.0                       # ускоряет DNS для aiohttp
polars==1.16.0                      # обработка данных в 10x быстрее pandas
numpy==2.2.3                        # матричные операции
numba==0.60.0                       # JIT-компиляция Python-циклов

# ----- КЭШ И БАЗЫ ДАННЫХ (мгновенный доступ) -----
redis==5.2.1                        # кэш коэффициентов и состояний моделей
asyncpg==0.30.0                     # асинхронный PostgreSQL
aioredis==2.0.1                     # асинхронный Redis
sqlalchemy==2.0.38                  # ORM + асинхронность

# ----- САМООБУЧЕНИЕ (онлайн + байесовский вывод + RL) -----
river==0.21.0                       # онлайн-обучение после каждого матча
pymc==5.20.0                        # байесовская оценка неопределённости
arviz==0.20.0                       # визуализация байесовских моделей
stable-baselines3==2.5.0            # Reinforcement Learning (стратегия ставок)
gymnasium==1.0.0                    # среда для RL
shap==0.46.0                        # объяснение предсказаний модели
optuna==4.2.0                       # автоматический подбор гиперпараметров

# ----- ПАРАЛЛЕЛИЗМ И РАСПРЕДЕЛЕНИЕ -----
ray==2.40.0                         # распределённые вычисления на всех ядрах
celery==5.5.0                       # очередь задач (фоновое обучение)

# ----- ЛОГИРОВАНИЕ И МОНИТОРИНГ -----
loguru==0.7.3                       # красивые логи с контекстом
sentry-sdk==2.22.0                  # отслеживание ошибок
prometheus-client==0.21.1           # метрики производительности
psutil==7.0.0                       # мониторинг CPU/RAM

# ----- ВЕБ-ИНТЕРФЕЙС (для webhook и дашборда) -----
fastapi==0.115.11                   # API для админки
uvicorn[standard]==0.34.0           # сервер для FastAPI

# ----- УТИЛИТЫ -----
click==8.1.8                        # CLI-команды для админа
tqdm==4.67.1                        # прогресс-бары при обучении
joblib==1.4.2                       # сохранение/загрузка ML-моделей
